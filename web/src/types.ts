export enum PlayerState {
    eliminated,
    ineligible,
    pendingRegistration,
    active,
}

export enum GameState {
    unpublished,
    preRegistration,
    registration,
    active,
    paused,
    completed,
}